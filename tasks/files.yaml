---
- name: Create files
  sudo: yes
  file:
    path: "{{ item.path }}"
    state: "{{ item.state }}"
    owner: "{{ item.owner }}"
  with_items: superslacker_create_files
  when: superslacker_create_files is defined
